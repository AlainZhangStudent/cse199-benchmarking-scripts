run volt_live_display_test to see the voltage drop in real time via different stages in stress testing (cpu, gpu)

run temp_live_display to see the temperature deltas in real time via the stress tests ran in benchmarks

run crash_test to attempt to crash the pi

in benchmarks,run cpu_benchmarks to see logged cpu data
